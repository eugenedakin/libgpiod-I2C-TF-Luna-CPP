# Set default behavior to automatically normalize line endings.
# This ensures all text files use LF endings in the repository,
# and Git converts to CRLF only when checking out on Windows.
* text=auto

# Explicitly declare C/C++ source code files as text, using the
# built-in 'cpp' diff language for better analysis.
*.c   text diff=cpp
*.cc  text diff=cpp
*.cxx text diff=cpp
*.cpp text diff=cpp
*.h   text diff=cpp
*.hh  text diff=cpp
*.hpp text diff=cpp

# Explicitly declare files that are always binary and should not be modified
# (e.g., no line ending conversion, no diff attempts).
*.png   binary
*.jpg   binary
*.jpeg  binary
*.gif   binary
*.ico   binary
*.lib   binary
*.a     binary
*.dll   binary
*.exe   binary
*.obj   binary
*.o     binary
*.gch   binary
*.pch   binary
*.sdf   binary
*.suo   binary
*.user  binary
*.ncb   binary
